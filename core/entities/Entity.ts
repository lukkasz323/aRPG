export class Entity {
    // parent: Entity = null;
    children: Entity[] = [];

    // constructor(parent?: Entity) {
    //     this.parent = parent ? parent : null;
    // }

    update(arg1?: any): void {}

    render(arg1?: any): void {}
}